name: build-macos-git-pkg

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"
  pull_request:

jobs:
  build:
    runs-on: macos-14

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Show toolchain
        run: |
          set -euxo pipefail
          sw_vers
          uname -a
          /usr/bin/make -v || true
          /usr/bin/make --version || true
          xcode-select -p || true
          clang --version || true

      # Install GNU make without Homebrew (runner may not have brew in PATH)
      - name: Build and install GNU make (local)
        run: |
          set -euxo pipefail
          MAKE_VER="4.4.1"
          cd "$RUNNER_TEMP"
          curl -fsSL -o make.tar.gz "https://ftp.gnu.org/gnu/make/make-${MAKE_VER}.tar.gz"
          tar xzf make.tar.gz
          cd "make-${MAKE_VER}"
          ./configure --prefix="$HOME/gmake"
          /usr/bin/make -j"$(sysctl -n hw.ncpu)"
          /usr/bin/make install
          echo "$HOME/gmake/bin" >> "$GITHUB_PATH"
          "$HOME/gmake/bin/make" --version

      # Optional: show Makefile lines around 43 (for sanity/debug)
      - name: Debug Makefile line 35-55
        run: |
          set -euxo pipefail
          nl -ba Makefile | sed -n '35,55p'

      - name: Build installer package (GNU make)
        run: |
          set -euxo pipefail
          make --version
          make clean || true
          make package

      - name: List outputs
        if: always()
        run: |
          set -euxo pipefail
          ls -lah
          ls -lah build || true
          ls -lah tmp || true
          ls -lah stage || true
          ls -lah stage-arm || true
          ls -lah stage-intel || true

      - name: Upload artifacts (pkg/dmg/zip)
        uses: actions/upload-artifact@v4
        with:
          name: git-osx-installer
          path: |
            git-*.pkg
            git-*.dmg
            git-*.zip
            build/**/*.pkg
            tmp/**/*.pkg
            stage/**/*.pkg
